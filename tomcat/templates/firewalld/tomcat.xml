<?xml version="1.0" encoding="utf-8"?>
<service>
  <short>tomcat</short>
  <description>tomcat port</description>
  <port protocol="tcp" port="{{tomcat_http_port}}" />
  <port protocol="tcp" port="{{tomcat_jmx_registry_port}}" />
  <port protocol="tcp" port="{{tomcat_jmx_server_port}}" />
</service>
