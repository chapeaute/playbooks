{{mongodb_log_dir}}/*.log {
    daily
    rotate 5
    compress
    dateext
    missingok
    notifempty
    sharedscripts
    copytruncate
    postrotate
        /bin/kill -SIGUSR1 `cat {{mongodb_data_dir}}/mongod.lock 2> /dev/null` 2> /dev/null || true
    endscript
}
