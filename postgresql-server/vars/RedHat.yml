---

postgresql_default_data_dir: /var/lib/pgsql/data
# postgresql_home_dir: "{{ postgresql_data_dir | dirname }}"
postgresql_home_dir: "/var/lib/pgsql"

postgresql_packages_default:
  - postgresql
  - postgresql-server
  - postgresql-contrib
  - python-psycopg2
