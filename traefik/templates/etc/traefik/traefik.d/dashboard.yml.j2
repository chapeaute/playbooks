http:
  routers:
    dashboard:
      rule: PathPrefix(`/`)
      service: api@internal
      entryPoints:
        - traefik
      middlewares:
        - dashboard-auth

  middlewares:
    dashboard-auth:
      basicAuth:
        users:
          - "{{ htpasswd.stdout }}"
